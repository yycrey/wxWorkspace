/* 全局样式 */
page {
  box-sizing: border-box;
}

/* 隐藏滚动条 */
::-webkit-scrollbar {
  display: none;
  width: 0;
  height: 0;
}

/* 全局页面容器 */
.page {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
}
